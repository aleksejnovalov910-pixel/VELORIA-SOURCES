{
  "name": "veloria-rp",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:server": "tsc -p src/server/tsconfig.json",
    "build:client": "tsc -p src/client/tsconfig.json",
    "build:cef": "pnpm --dir src/cef build",
    "build": "pnpm build:server && pnpm build:client && pnpm build:cef"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.3",
    "ioredis": "^5.7.0",
    "mysql2": "^3.14.4"
  },
  "devDependencies": {
    "@ragempcommunity/types-client": "^2.1.6",
    "@ragempcommunity/types-server": "^2.1.9",
    "@types/node": "^20.19.11",
    "typescript": "^5.9.2"
  }
}
