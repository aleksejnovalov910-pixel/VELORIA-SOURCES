import React from 'react';
export type HudState={health:number;armour:number;cash?:number;bank?:number;online?:number;vehicle?:{speed:number;engine:boolean;fuel?:number;seatbelt?:boolean}|null};
export function Hud({state}:{state:HudState}){return <div className="hud-root"><div className="hud-top"><b>VELORIA</b><span>ONLINE {state.online??0}</span></div><div className="hud-status"><span>HP {Math.max(0,state.health)}</span><span>ARM {Math.max(0,state.armour)}</span><span>${(state.cash??0).toLocaleString()}</span><span>BANK ${(state.bank??0).toLocaleString()}</span></div>{state.vehicle&&<div className="speedometer"><strong>{state.vehicle.speed}</strong><small>KM/H</small><div>{state.vehicle.engine?'ENGINE ON':'ENGINE OFF'} · FUEL {Math.round(state.vehicle.fuel??0)}%</div><div>{state.vehicle.seatbelt?'BELT ON':'BELT OFF'}</div></div>}</div>}
