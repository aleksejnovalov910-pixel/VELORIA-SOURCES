import React from 'react';
type PropertyItem={id:number;type:string;price:number;locked:boolean;garage_slots?:number;tax_amount?:number;tax_due_at?:string};
export function Property({items,onClose}:{items:PropertyItem[];onClose:()=>void}){return <div className="tablet-shell"><header><div><b>Недвижимость</b><small>{items.length} объектов</small></div><button onClick={onClose}>×</button></header><div className="data-grid">{items.map(p=><article className="data-card" key={p.id}><h3>{p.type} #{p.id}</h3><p>{p.locked?'Закрыто':'Открыто'} · Гараж: {p.garage_slots??0}</p><div className="stat-row"><span>Стоимость ${Number(p.price).toLocaleString()}</span><span>Налог ${Number(p.tax_amount??0).toLocaleString()}</span></div><small>Налог до: {p.tax_due_at??'—'}</small></article>)}</div></div>}
