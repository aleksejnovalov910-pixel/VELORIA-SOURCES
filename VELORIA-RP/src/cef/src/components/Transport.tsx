import React from 'react';
type Vehicle={id:number;model:string;plate:string;vin?:string;fuel:number;mileage?:number;oil?:number;battery?:number;insurance_until?:string};
export function Transport({vehicles,onClose}:{vehicles:Vehicle[];onClose:()=>void}){return <div className="tablet-shell"><header><div><b>Мой транспорт</b><small>{vehicles.length} автомобилей</small></div><button onClick={onClose}>×</button></header><div className="data-grid">{vehicles.map(v=><article className="data-card" key={v.id}><h3>{v.model}</h3><p>{v.plate} · VIN {v.vin??'—'}</p><div className="stat-row"><span>Топливо {Math.round(v.fuel)}%</span><span>{Math.round(v.mileage??0)} км</span></div><div className="stat-row"><span>Масло {Math.round(v.oil??100)}%</span><span>АКБ {Math.round(v.battery??100)}%</span></div><small>Страховка: {v.insurance_until??'нет'}</small></article>)}</div></div>}
