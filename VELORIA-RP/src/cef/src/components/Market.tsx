import React,{useMemo,useState} from 'react';
type Listing={id:number;category:string;title:string;price:number;quantity:number;status:string};
export function Market({items=[],onClose}:{items?:Listing[];onClose:()=>void}){const[category,setCategory]=useState('all');const[query,setQuery]=useState('');const filtered=useMemo(()=>items.filter(i=>(category==='all'||i.category===category)&&i.title.toLowerCase().includes(query.toLowerCase())),[items,category,query]);const call=(e:string,...a:unknown[])=>window.mp?.trigger(e,...a);return <div className="market-shell"><header><div><b>V-MARKET</b><small>Маркетплейс VELORIA</small></div><button onClick={onClose}>×</button></header><div className="market-tools"><input placeholder="Поиск" value={query} onChange={e=>setQuery(e.target.value)}/><select value={category} onChange={e=>setCategory(e.target.value)}><option value="all">Все</option><option value="vehicle">Транспорт</option><option value="property">Недвижимость</option><option value="clothes">Одежда</option><option value="item">Предметы</option></select></div><div className="market-list">{filtered.map(i=><div className="market-card" key={i.id}><div><small>{i.category}</small><strong>{i.title}</strong><span>Количество: {i.quantity}</span></div><div><b>${Number(i.price).toLocaleString()}</b><button onClick={()=>call('veloria:cef:market:buy',i.id)}>Купить</button></div></div>)}</div></div>}
